import InfoCard from "@/app/components/events/infoCard";

// TODO
const AdsCard: Component = () => {
    return (
        <InfoCard eventTitle={ "Stillingsannonser" }
                  showMoreUrl={ "/" } />
    )
}

export default AdsCard;
